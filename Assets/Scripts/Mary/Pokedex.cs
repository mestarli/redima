using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Pokedex : MonoBehaviour
{
    [SerializeField] private GameObject image_pokedex;

    [SerializeField] private GameObject text_pokedex;

    [SerializeField] private GameObject item_pokedex;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
